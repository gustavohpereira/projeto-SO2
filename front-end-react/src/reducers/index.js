// src/reducers/index.js
const initialState = {
    // seu estado inicial aqui
  };
  
  const rootReducer = (state = initialState, action) => {
    switch (action.type) {
      // defina seus casos de ação aqui
      default:
        return state;
    }
  };
  
  export default rootReducer;